# worker-pool-protocol

# Worker Pool Protocol

## Overview

The `worker-pool-protocol` objective is to ensure optimal utilization of resources while
processing a large number of tasks concurrently.

Check `pool.proto` for more information regarding this protocol

